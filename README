MFC - Math Fun with C
A C program intend to implement some maths so that it
may be inspeceted as assembly.  Will be playing with
BitInt implementation, prime finding, and maybe even
do some concurrency opperations


gcc -pthread main.c
